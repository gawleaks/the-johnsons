## Git rules

Do NOT push to git repository.

Commit messages should follow conventional commits rules, so start with type, for instance: "feat:" for features, "fix:" for bugfixes, etc.

## Coding style

Prefer functional over imperative. Many shorter funtions instead of bigger ones.

Pure functions when possible, but also classes can be used if this makes more sense.

Prefer map, filter, reduce over loops.